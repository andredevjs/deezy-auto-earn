const path = require('path');

const SAMPLE_FILE_PATH = path.join(__dirname, '../', '../', 'sample-config.json');
const CONFIG_FILE_PATH = path.join(__dirname, '../', '../', 'config.json');

module.exports = {
  SAMPLE_FILE_PATH,
  CONFIG_FILE_PATH,
};
